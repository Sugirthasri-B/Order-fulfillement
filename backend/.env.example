PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# Stage 2 / CHANGE1: Priority orders combining warehouses are "Partially
# Released" when at least this % of the requested quantity is available.
PRIORITY_PARTIAL_RELEASE_THRESHOLD_PERCENT=70

DB_SERVER=localhost
DB_PORT=1433
DB_NAME=MOBDB_DEV
DB_USER=sa
DB_PASSWORD=
DB_ENCRYPT=true
DB_TRUST_SERVER_CERTIFICATE=true
